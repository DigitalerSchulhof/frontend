type School {
  id: ID!
  name: String!
}

input SchoolSort {
  by: SchoolSortOption!
  direction: SortDirection!
}

enum SchoolSortOption {
  id
  name
}

input SchoolFilters {
  id: IDFilter
  name: StringFilter
}
